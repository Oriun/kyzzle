import { Integer, Bool, PgDate, JsonObject, Numeric, UnParametered, BoundedString, UnBoundedString, Timestamp, UserDefined, } from "./pg_types";
export { ArrayType, array } from "./array";
export const bigint = (name) => new Integer(name, { size: 8 });
export const int8 = bigint;
export const bool = (name) => new Bool(name);
export const boolean = bool;
export const date = (name) => new PgDate(name);
export const integer = (name) => new Integer(name, { size: 4 });
export const int4 = integer;
export const int = integer;
export const smallInt = (name) => new Integer(name, { size: 2 });
export const int2 = smallInt;
export const json = (name, schema) => new JsonObject(name, { mode: "json", schema: schema });
export const jsonb = (name, schema) => new JsonObject(name, { mode: "jsonb", schema: schema });
export const numeric = (name, parameters) => new Numeric(name, parameters);
export const decimal = numeric;
export const doublePrecision = (name) => new UnParametered(name, { type: "double precision" });
export const real = doublePrecision;
export const serial = (name) => new UnParametered(name, { type: "serial" });
export const serial4 = serial;
export const char = (name, { length }) => new BoundedString(name, { mode: "char", length });
export const varchar = (name, { length }) => new BoundedString(name, { mode: "varchar", length });
export const character = char;
export const characterVarying = varchar;
export const text = (name) => new UnBoundedString(name);
export const timestamp = (name) => new Timestamp(name, { withTimezone: false });
export const timestamptz = (name) => new Timestamp(name, { withTimezone: true });
export const uuid = (name) => new UnParametered(name, { type: "uuid" });
export const custom = (type, schema) => (name) => new UserDefined(name, { type, schema });
//# sourceMappingURL=helpers.js.map